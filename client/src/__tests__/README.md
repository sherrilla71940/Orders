In order to make the test run:

1. npm install --save-dev @testing-library/react
2. npm install --save-dev react-test-renderer
3. npm i --save-dev @testing-library/jest-dom
4. npm install redux-mock-store --save-dev 
5. npm install react-scripts --save-dev
4. npm run test